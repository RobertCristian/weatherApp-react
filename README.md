A Weather App side project I made by learning React. It uses OpenWeather API.

## Instalation

In the project directory, run:

`yarn install`

Make sure to add your OpenWeather API key to .env and run:

`yarn start`